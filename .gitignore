# Blender working state — large CAD sources; keep local / external backup
blender_files/
*.blend
*.blend1
_validation/
__pycache__/
*.py[cod]

# ...except the master scene, which is checked in via Git LFS
!reference/Johnny.blend
