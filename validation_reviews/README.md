# Validation Reviews

This directory contains concise milestone evidence only.

Current records:

- `geometry_repair/classification.json` — retained Repairs 001–013 and the
  reason local component repair was stopped.
- `repository_cleanup/master_promotion.json` — exact consolidation of the
  latest accepted fitted candidate and V28 prototypes into the tracked master.

Generated reports, renders, sanitation manifests, and exploratory notes stay
under ignored `.work/`. Promote a result here only when it supports a named
milestone or authority change.
